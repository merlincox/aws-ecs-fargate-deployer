import React from "react";

const Home = () => (
    <div>
        <h2>Home</h2>
        <p>Demonstrating a super simple React server-side rendering (SSR) single-page app (SPA) running on AWS Elastic Container Service (ECS) Fargate serverless Docker containers in the cloud.</p>
        <p>See <a href="https://github.com/merlincox/aws-ecs-fargate-deployer" target="_blank">https://github.com/merlincox/aws-ecs-fargate-deployer</a></p>
    </div>
);

export default Home;
